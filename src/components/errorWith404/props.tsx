import { ReactNode } from 'react';

export interface pageNotFound {
  pageNotFoundIcon: ReactNode,
  pageNotFoundText: string,
  goBackButton: object,
  ButtonBlockStyle: object,
  messageTextStyle: object,
  rootStyle: object,
  iconBlockStyle:object,
}
